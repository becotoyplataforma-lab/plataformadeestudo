-- ============================================================
-- ConcursoAI — Domínio: Analytics — Seeds (dev/staging apenas)
--
-- O domínio Analytics não possui catálogo próprio; as métricas derivam das
-- tabelas de origem (Study, AI, Identity). Este arquivo documenta esse fato
-- e não insere dados (nada a semear).
-- ============================================================

-- (sem seeds — dados derivados de question_attempts, study_tasks,
--  review_schedules, ai_usage e chat_messages)
SELECT 1;
