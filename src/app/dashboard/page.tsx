// /dashboard — reutiliza a página principal da área logada (grupo (dashboard)).
// Sem duplicação: o conteúdo (autenticação + resumo) continua em (dashboard)/page.tsx.
export { default } from "@/app/(dashboard)/page";
